Se utilizaron usuario admin clave admin
npm install i para instalar las dependencias 
npm run start para partir con el proyecto.
La función de mapa no está terminada.
SRC->COMPONENTES->están todos los códigos.
